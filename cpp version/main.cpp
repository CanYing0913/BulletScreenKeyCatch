#include "MomentCatch.cpp"

int main(){
    // pre-processed code, no file dependency
    set_save_category(kw1, c1, 0);
    set_save_category(kw2, c2, 0);
    set_save_category(kw3, c3, 0);
    set_save_category(kw4, c4, 0);
    //---------------------------------------
    std::string filename = "testFile.txt";
    find_file(filename);
}